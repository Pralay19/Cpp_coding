#include<bits/stdc++.h>
using namespace std;


int main(){
	vector<int>arr;
	int i;
	while(cin>>i)
	{
		arr.push_back(i);
	}
	int n=arr.size();
	vector<int>ans(n,0);

	for(auto p:ans){
		cout <<p<<" ";
	}
}